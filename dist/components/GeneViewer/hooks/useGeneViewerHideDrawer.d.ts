/// <reference types="react" />
/** Hide JBrowse's native menu bar and feature drawer so only our custom panel is used. */
export declare function useGeneViewerHideDrawer(
  viewState: any,
  containerRef: React.RefObject<HTMLDivElement | null>,
): void;
